<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <permission
        android:name="com.dccleaner.permission.READ_NOW_PLAYING"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_music_bridge"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DccleanerMusicBridge">
        <activity
            android:name=".MusicBridgeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MusicNotificationListenerService"
            android:label="@string/notification_listener_name"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <provider
            android:name=".NowPlayingProvider"
            android:authorities="com.dccleaner.musicbridge.nowplaying"
            android:exported="true"
            android:readPermission="com.dccleaner.permission.READ_NOW_PLAYING" />
    </application>
</manifest>
